include: package:very_good_analysis/analysis_options.11.0.0.yaml

plugins:
  riverpod_lint: 3.1.9

analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
    - '**/*.g.dart'
    - '**/*.freezed.dart'

linter:
  rules:
    public_member_api_docs: false
    # Style-only rules that do not affect runtime correctness. Keeping these
    # disabled avoids release gates failing on equivalent constructor syntax
    # or explicitly stated default argument values.
    lines_longer_than_80_chars: false
    cascade_invocations: false
    prefer_const_constructors: false
    avoid_redundant_argument_values: false
