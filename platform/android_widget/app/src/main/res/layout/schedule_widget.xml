<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/schedule_widget_background"
    android:clipChildren="true"
    android:clipToPadding="true">

    <!--
      StackView displays exactly one active schedule card while retaining its
      native flick gesture. Rotating the collection by 90 degrees maps that
      gesture to a physical left/right swipe on the launcher. The provider
      swaps the measured width/height at runtime so this touch surface covers
      the whole visible widget instead of only a narrow strip.
    -->
    <StackView
        android:id="@+id/widget_list"
        android:layout_width="88dp"
        android:layout_height="320dp"
        android:layout_gravity="center"
        android:animateFirstView="false"
        android:clickable="true"
        android:clipChildren="true"
        android:clipToPadding="true"
        android:loopViews="true"
        android:rotation="90" />

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:paddingStart="18dp"
        android:paddingEnd="18dp"
        android:text="Không có lịch học sắp tới"
        android:textColor="#FFFFFFFF"
        android:textSize="14sp"
        android:textStyle="bold" />
</FrameLayout>
