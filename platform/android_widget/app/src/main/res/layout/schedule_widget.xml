<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/schedule_widget_background"
    android:clipChildren="true"
    android:clipToPadding="true">

    <!--
      AppWidget RemoteViews does not support ViewPager. A ListView is rotated
      90 degrees so its native vertical drag becomes a horizontal drag on the
      launcher. Unlike StackView, ListView renders one full-size row after the
      next instead of compositing several cards on top of each other.
    -->
    <ListView
        android:id="@+id/widget_list"
        android:layout_width="88dp"
        android:layout_height="320dp"
        android:layout_gravity="center"
        android:clipToPadding="false"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:fadeScrollbars="false"
        android:fadingEdge="none"
        android:overScrollMode="never"
        android:rotation="90"
        android:scrollbars="none" />

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:paddingStart="18dp"
        android:paddingEnd="18dp"
        android:text="Không có lịch học sắp tới"
        android:textColor="#FFFFFFFF"
        android:textSize="14sp"
        android:textStyle="bold" />
</FrameLayout>
