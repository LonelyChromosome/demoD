<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/schedule_widget_background"
    android:clipChildren="true"
    android:clipToPadding="true">

    <!--
      Use StackView in its native vertical direction. A rotated StackView makes a
      horizontal gesture compete with the launcher's home-screen page gesture.
      Native vertical swiping keeps collection gestures inside the widget while
      preserving the same stacked-card behaviour and data source.
    -->
    <StackView
        android:id="@+id/widget_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:animateFirstView="false"
        android:clickable="true"
        android:clipChildren="true"
        android:clipToPadding="true"
        android:loopViews="true" />

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:paddingStart="18dp"
        android:paddingEnd="18dp"
        android:text="Không có lịch học sắp tới"
        android:textColor="#FFFFFFFF"
        android:textSize="14sp"
        android:textStyle="bold" />
</FrameLayout>
